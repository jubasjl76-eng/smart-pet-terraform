region = "eu-west-1"

# Set a real domain to get api./mqtt. records + ACM certs (HTTPS/TLS).
# zone_name   = "smartpet.example"
# create_zone = true

# alarm_email = "ops@smartpet.example"

# prod broker needs an auth-enabled image (EMQX or mosquitto + dynamic-security);
# eclipse-mosquitto with allow_anonymous=false rejects everyone.
# broker_image = "<acct>.dkr.ecr.eu-west-1.amazonaws.com/smart-pet/broker:latest"
